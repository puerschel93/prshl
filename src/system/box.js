import styled from "styled-components";
import { layout, flexbox, color, border, space, position } from "styled-system";

const Box = styled.div(layout, flexbox, color, border, position, space);

export default Box;
